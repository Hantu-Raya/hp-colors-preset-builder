---
import PresetBuilderIsland from '../components/PresetBuilderIsland.jsx';
import { getGitCommitInfo } from '../gitCommitInfo.js';
import '../styles/global.css';

const gitCommitInfo = getGitCommitInfo();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>HP Colors Game Menu Builder</title>
    <meta
      name="description"
      content="Build HP Colors presets with the same navigation used by the in-game menu."
    />
    <meta property="og:title" content="HP Colors Game Menu Builder" />
    <meta
      property="og:description"
      content="Build HP Colors presets with the same navigation used by the in-game menu."
    />
    <link rel="icon" href="data:," />
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>
    <main id="main-content" class="shell">
      <PresetBuilderIsland gitCommitInfo={gitCommitInfo} client:load />
    </main>
  </body>
</html>
